import React from 'react';


const Directions = props => {
  return (
    <div>
      <p>
      To buzz in click on the left(Player 1) or right(Player 2) shift button on the keyboard
      </p>
    </div>
  )
}

export default Directions;
